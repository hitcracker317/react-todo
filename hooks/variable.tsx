/*
*
* color style
--------------------- */
const colorPrimary = () => {
  return `#553EE3;`
}

const colorBackgroundStatus = () => {
  return `#FBFBFB;`
}

const colorLine = () => {
  return `#FFF;`
}

const colorPrimaryBackground = () => {
  return `#EDE1FF;`
}

const colorPatagraph = () => {
  return `#321D60;`
}

const colorBaseBackground = () => {
  return `#F8F3FF;`
}

const colorSecondaryBackground = () => {
  return `#F5EEFF;`
}

const colorHightlight = () => {
  return `#DBC4FE;`
}

const colorAnnotation = () => {
  return `#855EC0;`
}

const colorPlaceholder = () => {
  return `#AD98FF;`
}

/*
*
* effect style
--------------------- */
const effectShadow4 = () => {
  return `0px 4px 4px rgba(229, 214, 250, 0.8);`
}
const effectShadow8 = () => {
  return `-4px 0px 4px rgba(229, 214, 250, 0.8);`
}

export const Colors = {
  colorPrimary,
  colorBackgroundStatus,
  colorLine,
  colorPrimaryBackground,
  colorPatagraph,
  colorBaseBackground,
  colorSecondaryBackground,
  colorHightlight,
  colorAnnotation,
  colorPlaceholder
}

export const Effects = {
  effectShadow4,
  effectShadow8
}